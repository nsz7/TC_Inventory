# Copy this file to .env and fill in real values.

# PostgreSQL connection string used by the API server and drizzle-kit.
DATABASE_URL=postgresql://tc_inventory:tc_inventory@localhost:5432/tc_inventory

# Port the API server listens on. Defaults to 3000 if unset.
PORT=3000
